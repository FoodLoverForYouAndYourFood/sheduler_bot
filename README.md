TaskPair — минимальный Telegram-бот для трекинга задач вдвоём. Хранит задачи в SQLite, даёт короткие команды и каждое утро шлёт дайджест.

## Быстрый старт
- Установи Python 3.11+.
- Создай окружение и поставь зависимости: `python -m venv .venv && .venv\\Scripts\\activate && pip install -r requirements.txt`.
- Скопируй `config.example.json` в `config.json` и пропиши свои данные.
- Задай токен: положи его в `.env` (`BOT_TOKEN=...`) или установи вручную `set BOT_TOKEN=...` / `export BOT_TOKEN=...`.
- Запусти бота: `python bot.py`.

## Настройка `config.json`
- `report_time`: во сколько высылать утренний отчёт (`HH:MM` в указанном часовом поясе).
- `timezone`: часовой пояс, например `Europe/Moscow`.
- `users`: два пользователя, у каждого `id` (из Telegram), `alias` (короткая буква) и `name` (как показывать в списках).

Пример смотри в `config.example.json`.

## Команды
- `/add <текст>` — добавить задачу (бот спросит исполнителя, приоритет, дедлайн).
- `/todo`, `/doing`, `/done` — списки по статусам.
- `/all` — все задачи.
- `/me` — только мои задачи.
- `/update <id> <todo|doing|done>` — сменить статус.
- `/report` — прислать утренний отчёт вручную.
- `/cancel` — отменить ввод задачи.

## Как это работает
- Статусы: `🟦 TODO`, `🟧 DOING`, `🟩 DONE`.
- Приоритеты: high / medium / low.
- Дедлайн необязателен (просто напиши `skip`).
- Утренний отчёт собирает TODO, DOING и DONE за последние 24 часа и рассылается обоим пользователям из `config.json`.
- База лежит в `tasks.db` рядом со скриптом.
